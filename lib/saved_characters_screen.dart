import 'package:flutter/material.dart';

class SavedCharactersScreen extends StatelessWidget {
  const SavedCharactersScreen();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Saved Screen",
      ),
    );
  }
}
